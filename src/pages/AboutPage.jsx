function AboutPage() {
  return (
    <section className="details-card">
      <span className="eyebrow">About</span>
      <h1>Car Marketplace starter</h1>
      <p>
        This project currently includes only the initial scaffold and route
        structure for the marketplace app.
      </p>
    </section>
  );
}

export default AboutPage;
